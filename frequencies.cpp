#include <iostream>
#include <string>
#include <cstdlib>
#include <vector>
#include <fstream>

std::vector<std::string> newSeq;
for(unsigned long i = 0; i<= seq.length(); i ++){
             //Makes trigram the size of 3
             tri += seq[i];
             tri += seq[i+1];
             tri += seq[i+2];
             if (Condon.length() < 3){
                    //returns Amino if the length of Condon is shorter than three
                    return newSeq;
                    break;
             }
             //Find the Amino Acid
             else{
